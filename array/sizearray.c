#include<stdio.h>
int cal_sum(const int arr[] , int size)
{
	int i = 0;
	int sum = 0;
	for( i = 0; i < size ; i++){
	printf("\n input :");
	scanf("\n %d", a[i]);
	}
	for ( i = 0 ; i < size; i++)
		sum += arr[i];
	return sum ;
}
int main()
{ 
int size;
const int arr[];
printf("\n %d ",cal_sum(arr[],size); 
		return 0;
		}
